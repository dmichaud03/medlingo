type Props = {
    activeCourse: any // TODO: Replace with Database types
    hearts: number;
    points: number;
    hasActiveSubscription: boolean;
};

export const UserProgress = ({}: Props) => {
    return (
        <div>
            User Progress
        </div>
    );
};